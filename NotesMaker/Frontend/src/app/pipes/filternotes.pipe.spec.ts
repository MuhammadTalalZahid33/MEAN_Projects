import { FilternotesPipe } from './filternotes.pipe';

describe('FilternotesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilternotesPipe();
    expect(pipe).toBeTruthy();
  });
});
